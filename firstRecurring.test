/*
Given an array, return the first recurring character. Basically we need to build a function that takes an array and it’s going to look through all the numbers and finds which one gets repeated first.

For example :

Given an array =[2, 4, 5, 1, 2, 5, 3, 7]
It should return 2, because 2 repeated first comparing with other numbers.

Given an array =[2, 1, 1, 3, 2, 5, 3, 7] --> 1

Given an array =[2, 4, 5, 1, 2, 5, 3, 7]

Given an array =[2, 4, 1, 7] --> returns undefined
*/
const firstRec = require("./firstRecurring");

it("[2, 4, 5, 1, 2, 5, 3, 7]", () => {
    expect(firstRec([2, 4, 5, 1, 2, 5, 3, 7])).toBe(2);
})

it("[2, 1, 1, 3, 2, 5, 3, 7]", () => {
    expect(firstRec([2, 1, 1, 3, 2, 5, 3, 7])).toBe(1);
})

it("[2, 4, 5, 1, 8, 7, 3, 4, 3]", () => {
    expect(firstRec([2, 4, 5, 1, 8, 7, 3, 4, 3])).toBe(4);
})

it("[2, 4, 1, 7]", () => {
    expect(firstRec([2, 4, 1, 7])).toBe(undefined);
})